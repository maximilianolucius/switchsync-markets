# arXiv submission notes — SwitchSync Markets (P1.3)

**Title:** SwitchSync Markets: A Preregistered Synthetic Stress Test of Switching
Synchronization as a Financial-Network Mechanism

**Suggested categories:** nlin.AO (primary); q-fin.ST, physics.soc-ph (cross-list).

## Contents

| file | role |
|---|---|
| `main.tex` | the paper (standard `article` class; 14 sections + 2 appendices) |
| `references.bib` | bibliography — every entry transcribed verbatim from the project's verified source ledger (`docs/research/source_ledger.csv`); no invented citations or DOIs |
| `figures/fig_*.pdf` | five vector figures, generated by `figures/make_figures.py` **directly from the frozen JSON reports** (no simulation re-run) |
| `figures/make_figures.py` | figure generator (reads `results/p1_2f_cheap_suite/1baa47da06fede2a/*.json`) |
| `tables/table_*.tex` | gate-results, inference, G0B and audit tables (`\input` by `main.tex`) |
| `main.pdf` | compiled output (pdflatex + bibtex via latexmk) |

## Build

```sh
latexmk -pdf main.tex
```

Packages used (all standard TeX Live): geometry, amsmath, amssymb, graphicx,
booktabs, natbib, microtype, xcolor, hyperref. No proprietary or nonstandard
fonts; figures embed TrueType (fonttype 42).

## Scientific-discipline checklist (enforced in the text)

- G0B is described as a **calibrated qualitative demonstration**, never an exact
  reproduction, with no N-dependence claim.
- No claim that switching beats the average graph; the diagnostic G1-strict
  values (negative in 8/8 seeds) are labelled NOT_INTERPRETABLE and never promoted.
- G1-weak is reported as **directional evidence (8/8 positive, p=0.0078) that
  failed the preregistered magnitude criterion** (INCONCLUSIVE/TIE).
- The asynchronous experiment is called **asynchrony/LOCF (Epps-like
  degradation)**, not a causal Epps demonstration.
- No trading, price-prediction or market-causality applicability is claimed.
- "No real market data were used" is stated in the abstract, title footnote,
  Section 11, Appendix A and the conclusion.
- The negative result is explicitly **not** a refutation of the seed paper's
  mathematics (Eser, Medeiros, Riza & Engel, arXiv:2507.08007).

## Provenance

All numbers in the paper come from the sealed attempt `1baa47da06fede2a`
(execution freeze v8, content hash `2991e064…`), committed byte-for-byte under
`results/p1_2f_cheap_suite/` (tag `switchsync-synthetic-results-v2`) and verified
by an independent audit. Superseded v1 reports were **not** used anywhere.

## Before submission (manual steps)

- Replace the project byline with the personal author name(s) and affiliation.
- Choose a license for the arXiv submission (repository license selection is
  still pending — `docs/LICENSE_BLOCKER.md`; do not resolve unilaterally).
