# DECISIONS (append-only)

Scientific decisions, contract choices, and their reasons. Newest entries appended at the bottom. Do not edit past entries.

---

## 2026-07-13 — Project initialized (P0–P1)

- **D1. New independent project.** SwitchSync Markets is created fresh; it is not a continuation of any prior trading work. No prior data or code is imported.
- **D2. Paper fixed and hashed.** arXiv:2507.08007v2 downloaded to `docs/research/sources/`, SHA-256 `0774a03c9c5f84fa1c6bb6c70a1a13345b23e3e3ecf5cb37a4b882b085e442e6`. Audit reads v2 in full.
- **D3. `G_switch` demoted.** The connectivity gap `λ2(avg) − mean λ2(snapshot)` is retained only as an "aggregation connectivity gap" descriptor. Reason: proven invariant under time-dilation (rate-blind) and under reordering (order-blind); therefore cannot be the dynamic switching mechanism. Primary metrics are the order-sensitive propagator `γ` (linear) and the transverse Lyapunov exponent `Λ_⊥` (nonlinear), both defined from the full variational dynamics including the local Jacobian.
- **D4. Two translations separated.** Faithful multilayer (T1: same asset, two venues, basis = transverse coordinate, sync manifold ≈ cointegration) is the primary target. Broad generalization (T2: different assets, directed/signed links) is treated as new theory, used only as a synthetic stress test, never presented as "the paper applied to markets".
- **D5. Directed/signed operators.** For directed/signed surrogates the symmetric-Laplacian `λ_2` is forbidden; the ordered product of the actual (possibly non-normal) per-step maps and its largest singular value are used instead.

## 2026-07-13 — Reproduction parameter departure (documented, not result-hacking)

- **D6. `σ_12 = 1.5` for the FHN reproduction (paper uses 0.1 at N=400).** Reason discovered empirically: inter-layer coupling acts only on the activator, and full static coupling of two identical chaotic layers at our tractable size (N=40) synchronizes only for `σ ≳ 0.5`; with 25%-density switching the fast-limit effective coupling is ≈0.25×, so the sync threshold sits near `σ_12 ≈ 1.0–1.5`. Gate G0 is defined as a *qualitative* reproduction (fast syncs, slow does not, finite max switching time), not a match of the absolute threshold. Documented in the audit and the prereg. The mechanism (fast-vs-slow separation, threshold, monotonic `T_swt` dependence) reproduces cleanly.
- **D7. Benettin tangent co-integration.** First implementation used a frozen-Jacobian explicit-Euler tangent and produced a spurious negative `λ_max`. Fixed to co-integrate state and tangent with RK4, evaluating the Jacobian at each substage. Verified `λ_max > 0` at N∈{40,100,200} (≈0.006–0.017), consistent with the paper's ~0.04. This was a bug fix, not a tuning choice.

## 2026-07-13 — Identifiability estimator (fair-attempt fix, criteria unchanged)

- **D8. Estimator changed from rolling basis-variance to rolling AR(1) coefficient; window reduced 24→8 to match the dwell.** Reason: a window much longer than the dwell straddles multiple on/off cycles and washes out the signal; the AR(1) self-coefficient of the venue-difference basis cleanly separates active (mean-reverting, coef≈A_jj−2κ<0) from inactive (expanding, coef≈1.04). This makes the estimator a *fair* attempt rather than a strawman. **Acceptance criteria (P/R>0.6, corr>0.5) were NOT changed.** The estimator still fails the bar (P/R≈0.35), and that FAIL is reported honestly.

## 2026-07-13 — Minimal-system MSF limitation (recorded, not forced)

- **D9. Minimal-system MSF does not reproduce the slow-switching instability.** The N=2-per-layer isolated layer is a limit cycle (`λ_max ≈ -0.001`, verified), so its transverse mode does not expand during off-phases and any switched coupling contracts (`Ψ<0` for all tested `σ,T_swt`). The switching-TIME dependence of the stability boundary is therefore NOT reproduced in the minimal build; it IS reproduced by the large-N direct simulation (G0). MSF verdict recorded as PARTIAL with this explanation rather than overclaimed as PASS.

## 2026-07-13 — Honest gate reading

- **D10. G1 strict bar and G2 favor aggregate connectivity.** The average (time-averaged) graph contracts more than fast switching in the surrogate (`γ_avg > γ_fast`), and temporal order adds nothing beyond the seed-noise band. Per the brief this is an allowed, non-pejorative outcome: evidence favors aggregate connectivity over dynamic switching. The defensible positive content is that fast switching lets a *sparse* system emulate the *dense average*, beating the equal-density static-sparse graph. Recorded rather than reframed as success.

## 2026-07-13 — P1.1 corrective audit, v2 contract, v2 freeze

- **D11. Independent audit of v1 (13 defects).** `docs/audits/p1_v1_independent_audit.md`. Each defect verified against code/recomputation. Dispositions: G0 → calibrated demonstration; G1/G2 → superseded pending paired rerun; G3-signed & H3 → invalid; G4 → FAIL on P/R only (`contraction_corr` invalid); MSF → invalid; freeze v1 → non-executable. v1 reports preserved byte-for-byte; supersession recorded only in `*.superseded.json` sidecars. No v1 commit or tag rewritten.
- **D12. Withdrew the "switching cannot beat aggregate connectivity" over-claim.** Averaging theory constrains only the fast limit. Zhang–Strogatz (Nature Comms 12, 3273, 2021) show a budget-constrained, MSF-curvature-driven temporal advantage at intermediate rates. `literature_review.md`/`novelty_matrix.md` corrected; N2/N3 moved from "Refuted" to "NOT ESTABLISHED"; a separate unexecuted intermediate-rate branch prereg added. This is NOT a reinterpretation of v1 as positive — the questions are simply reopened.
- **D13. Paired rate contrast (C1).** `src/networks/paired_switching.py`: fast/intermediate/slow derived from ONE base snapshot sequence per seed by cycle count, EXACT horizon H=240 (no `H//dwell+1`), identical time-averaged operator asserted. Replaces the v1 unpaired 242/252/300 arms.
- **D14. Real nulls (C2).** `variable_dwell_schedule` gives a frozen non-constant dwell distribution so `shuffle_dwell` is a genuine null (v1 was a no-op).
- **D15. MSF anti-phase (C3).** `msf_switching.smooth_square_gamma_v2` shifts the second channel by a half period `T_swt` (v1 used a full period `2·T_swt` → identical channels).
- **D16. Genuine signed operator (C4).** `surrogate_v2.build_basis_operator_v2` introduces ≥1 strictly negative coupling with preserved off-diagonal Frobenius budget and reported sign metadata.
- **D17. H3 removed (C5).** A single-variable coverage-vs-reachability contrast is not achievable for the mirror-pair matching; H3 dropped as a gate; `temporal_reachability_ratio` kept as a reported descriptor with no isolation claim.
- **D18. Same-realization ground truth (C6).** `surrogate_v2.simulate_observed_v2` records `d_true`; `contraction_corr_same_realization` uses it. v1 correlated against an independent trajectory.
- **D19. G0 split (C7).** G0A exact-paper (σ=0.1, may be INCONCLUSIVE_BY_COST) vs G0B calibrated demonstration (σ=1.5, N=40) vs G0C minimal MSF. G0A is never silently replaced by G0B.
- **D20. Executable freeze v2 (E).** `src/validation/freeze_v2.py`: path-independent content hash (stored==recomputed), added/missing/modified detection, environment verification, refuse-overwrite, atomic write, verifier never regenerates. Covers src + runners + `_common` + tests + prereg (json+md) + eval/figures + lockfiles + ledger + contract docs + PDF SHA + git commit + environment.
- **D21. License is a BLOCKER.** No license invented; `docs/LICENSE_BLOCKER.md` documents the pending owner decision and that the bundled arXiv PDF keeps its own terms regardless.

## 2026-07-13 — P1.2 execution runners and execution freeze

- **D22. `switchsync-synthetic-prereg-v2-freeze` was a PREREG / MODULE freeze, not an EXECUTION freeze.** It froze the corrected modules, the v2 contract, the audit, and the tests, but it did **not** contain operative v2 runners — so nothing could be executed reproducibly against it. That tag and its commit (`d719e49`) are left untouched. The true **execution freeze** is built in P1.2 as a new manifest and a new annotated tag `switchsync-synthetic-execution-v2-freeze`, after the v2 runners exist. No prior tag is reused or moved.
- **D23. v2 runners are new files, isolated from superseded v1 implementations.** `experiments/run_g0a_exact_v2.py`, `run_g0b_calibrated_v2.py`, `run_g0c_msf_v2.py`, `run_g1_g2_paired_v2.py`, `run_g3_v2.py`, `run_g4_v2.py`, `run_suite_v2.py`. A test statically forbids importing the v1 runners, `src.validation.freeze` (v1), `linear_surrogate.build_basis_operator`/`simulate_observed` (v1), and `run_msf.smooth_square_gamma` (v1).
- **D24. `surrogate_v2.build_basis_operator_v2` finalized to be Laplacian-consistent and self-contained.** The P1.1 version used an adjacency-only off-diagonal (no diffusive self-term) and imported the v1 unsigned operator in `simulate_observed_v2`. Finalized so the unsigned/heterogeneous/directed path equals the intended diffusive operator `diag(base) + intra_coupling*L` (matching v1's faithful operator) and the signed path flips a fraction of off-diagonal couplings to strictly negative with the off-diagonal Frobenius budget preserved. No import of the v1 signed-buggy `build_basis_operator`. This changes the file hash relative to the prereg-v2-freeze manifest (expected; the execution freeze records the final hashes).
- **D25. Execution contract per runner.** `--i-am-authorized` + exact prereg canonical hash + prereg file SHA + execution-freeze content hash are all required for a full run; the freeze is verified before any computation; contract failures exit nonzero and write no report; reports are `*_v2.json`, atomic, refuse-overwrite, and carry full provenance (prereg hashes, freeze content hash, runner SHA, commit SHA, environment, seeds, params, gate, criterion) plus a verdict in {PASS, FAIL, INCONCLUSIVE, EXECUTION_INVALID}. `--dry-run` executes nothing.
- **D26. No experiments executed in P1.2.** Only tests, py_compile, dry-runs, tiny synthetic fixtures, and hash/freeze verifiers were run. Full grids, G0-G4 at contract scale, the Zhang-Strogatz branch, real data, and push are NOT done.
- **D27. v2 contract finalized for execution (`surrogate_paired` block added).** The P1.1 v2 JSON defined the surrogate gate criteria but lacked the concrete paired-construction grid (N, N_IL, K, H, cycles, variable-dwell multiset, best-static search, signed neg-fraction). P1.2 added a `surrogate_paired` block, changing the v2 canonical hash `7e5f1536…` → `be52bf96…` (file SHA `2607…` → `1e63787a…`); a further edit adding G0A `record_every` and G0C timing fields produced the final hashes above. The historical prereg/module tag `switchsync-synthetic-prereg-v2-freeze` (old hash) is untouched; the v2 runners require the new hash and the execution freeze records it.

## 2026-07-13 — P1.2-A pre-execution audit and lineage repair

- **D28. Four-hash resolution (forensic).** Frozen prereg v2 (canonical 7e5f1536, file 2607, blob 750b2fd, commit b940652/tag d719e49) was mutated in P1.2 to canonical be52bf96 (file 1e63787a, blob cd29737, commit 00a4c44). The diff shows ONLY execution parameters + a seed block were added; no gate criterion/estimand/acceptance/tolerance changed (Case 2). No v2 result was ever produced.
- **D29. Lineage repair.** Restored synthetic_prereg_v2.json/.md to their frozen bytes (canonical 7e5f1536); marked SUPERSEDED via `synthetic_prereg_v2.SUPERSEDED.md` (tag `switchsync-synthetic-prereg-v2-freeze` NOT rewritten). Created the scientific `synthetic_prereg_v3.json` (canonical a3de3d19, file 7aa8b226) and the operational `synthetic_execution_contract_v1.json` (canonical c519857a, file f9fd8c26, binding prereg v3). Changelog `docs/methodology/prereg_v2_to_v3_changelog.md`.
- **D30. Statistical contract closed (F) => prereg v3.** Added experimental unit, paired within-seed comparison, estimators, a bootstrap-95%-CI decision rule (n_boot=2000, boot_seed=20260713; a sign test on 8 seeds cannot reach p<0.05), band=max(3*std,0.02), tie rule, gate hierarchy, multiplicity, failed-run handling (>20% failed => EXECUTION_INVALID). Best-static policy: SELECTION on `paired_selection` [41-48], EVALUATION on disjoint `paired_evaluation` [81-88]; the winning static subset is frozen before evaluation (fixes v1 optimize-and-evaluate-on-same-realization).
- **D31. Dual-document contract + freeze identity (B).** `_contract_v2` now requires BOTH prereg (v3) and execution-contract (v1) canonical+file hashes, the freeze content hash, AND freeze identity: clean tree, HEAD==freeze_commit, freeze_tag derefs to freeze_commit, HEAD not a descendant of freeze_commit, manifest verifies. Reports record prereg+exec hashes, freeze content hash, runner SHA, source_commit, freeze_commit, freeze_tag, runtime_head.
- **D32. G0A cost rule (C) + verdict vocabulary.** INCONCLUSIVE_BY_COST is a reason_code (verdict=INCONCLUSIVE), not a verdict; NOT_RUN is a suite state. Frozen wall-time/size/seed budget, timeout->INCONCLUSIVE(INCONCLUSIVE_BY_COST) with completed-cell checkpoint, partial grid never PASS/FAIL, append-only checkpoints, no post-hoc budget revision. run_suite_v2 modes: --plan / --cheap-only / --include-g0a-expensive; G0A NOT_RUN by default.
- **D33. Runtime v1 isolation (E).** Beyond the AST scan (extended to ban importlib in v2 files), a runtime-poison test monkeypatches the superseded v1 functions to raise and asserts v2 computes still succeed; a subprocess import-graph test asserts no v1 runner (or src.validation.freeze) enters sys.modules when v2 runners are imported. New execution freeze v3 + annotated tag `switchsync-synthetic-execution-v3-freeze` (prior tags untouched).

## 2026-07-13 — P1.2-B pre-execution correction and execution freeze v4

- **D34. Execution freeze v3 -> SUPERSEDED / NONEXECUTABLE / NEVER RUN.** Independent audit (`docs/audits/p1_2a_independent_audit.md`) found it scientifically non-executable despite correct internal hashes and 80 passing tests. Sidecar `artifacts/freeze_execution_v3.SUPERSEDED.md`; tag/commit `69c542b` preserved, not rewritten. Empty/tautological v3 tests recorded (e.g. the `g0 - g0` MSF assertion) and replaced.
- **D35. prereg v4 + execution contract v2.** Scientific prereg v4 (canonical 847a8a8c, file 36dd76e0) and operational execution contract v2 (canonical 8738b591, file 919a3d8b, binds v4). Changelog `docs/methodology/prereg_v3_to_v4_changelog.md`. prereg v3 / exec v1 preserved.
- **D36. Single shared inference (C).** `src/metrics/inference.py:paired_decision` — exact two-sided sign test (8/8 -> p=0.0078125, correcting the false v3 claim), band=max(3*sample_std_ddof1, floor), explicit zero drop, alpha=0.05; bootstrap is a descriptor only. Used by G1_weak/G1_strict/G2/G3/G4-baseline.
- **D37. G1 arm + comparator (B).** Rate arm selected on selection seeds and FROZEN (no per-eval-seed max); comparator = best-of-frozen-candidate-set (NOT best admissible static); canonical candidate ordering + lexicographic tie-break; G1_strict & G2 NOT_INTERPRETABLE unless G1_weak PASS.
- **D38. G2 permutation null (D).** median of g2_n_perm frozen order-permutations per seed; signed paired test on gamma_ordered - median; no abs-vs-zero.
- **D39. G3 (E).** shared inference per stage; per-seed metadata for ALL seeds; failed-run handling; faithful+mild gate; signed stage verified per seed for a strictly negative weight AND numeric pre-rescale off-diagonal budget equality vs the unsigned comparator (surrogate_v2 now exposes offdiag_frobenius_prescale).
- **D40. G4 (F).** exact-horizon assertion (schedule.total_steps == H via H//dwell); four PASS conditions incl. a defined beats-baseline (paired precision margin, shared rule); >20% failed/nonfinite -> EXECUTION_INVALID.
- **D41. G0A (G).** durable append-only checkpoint OUTSIDE the repo (`_custody.CheckpointLedger`: fsync, crash-recovery, duplicate-reject, corruption-detect); chunked wall-clock deadline (simulate abort_check); frozen quantifiers (deciding sizes {200,400}, N=100 non-deciding, chaos prerequisite, ALL-cells rule); partial grid -> INCONCLUSIVE(INCONCLUSIVE_BY_COST).
- **D42. Custody (I).** external --run-dir, run_id = sha256(prereg|exec|freeze|freeze_commit)[:16]; transactional suite (staging -> atomic publish, failure ledger, no partial success bundle); reports carry runner + orchestrator SHA; sequential individual runners share run_id; --cheap-only/--include-g0a-expensive with rejected contradiction; no overwrite/resume/retry. Results never inside the frozen repo.
- **D43. Execution freeze v4 + tag `switchsync-synthetic-execution-v4-freeze`.** Separate source and freeze commits; stored content_hash == independent recomputation; prior tags not moved. 86 tests pass. No v2/v3/v4 result produced; NO-RUN; NO-PUSH.
